Folder_Scrapper
===============

Open source file scrapper for further manipulation and segmentation of files


Packages used : eyeD3

Usage:
python scrapper /path/to/folder


add-ons :
if you want to add a script to work on a file you need to create a module inside the "scripts_per_file" folder
if the folder name starts with an underscore it doesn't run the script over the file